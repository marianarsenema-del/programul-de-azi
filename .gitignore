# Fișiere cu date reale, secrete — NU se urcă niciodată pe GitHub
.env
lib/place-status-engine/.env

# dependințe instalate (se regenerează din package.json + package-lock.json
# cu "npm install", nu trebuie ținute în git — ar umple repo-ul degeaba)
node_modules/

# fișiere de sistem, specifice fiecărui calculator
.DS_Store
Thumbs.db

# loguri, dacă vreodată scriptul le generează
*.log
